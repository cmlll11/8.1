# 浅层诱导特征复杂度正向实验计划

> 版本：v1.0
> 日期：2026-08-01
> 本地目录：`D:\codex\backdoor-reverse\4`
> GitHub 仓库：<https://github.com/cmlll11/8.1>

## 1. 实验定位

本轮只进行**正向验证实验**：干净模型、后门模型、全局对抗映射 UAP 和后门 Trigger 全部已知并固定。

本轮不做以下工作：

- 不搜索或逆向 Trigger；
- 不重新训练 UAP 或 Trigger；
- 不直接在特征空间优化 ASR；
- 不接入 BetterTI、FeatureRE、DeBackdoor 等逆向方法；
- 不直接研究未知模型的后门检测。

本轮要先验证两个基础假设：

1. 不同 Trigger 在像素空间差异较大，但它们在浅层特征空间引起的变化可能更相似、更集中，从而更便于后续搜索。
2. 在相同拟合精度下，后门 Trigger 诱导的浅层特征映射可能比 UAP 诱导的浅层特征映射需要更少的描述 bit。

只有这两个假设都得到支持，才进入后续的反向特征搜索实验。

## 2. 基本定义

设：

- `x`：正常且不属于目标类别的输入样本；
- `C_c`：干净模型；
- `C_b`：后门模型；
- `p(x)`：已知 UAP 或全局对抗生成器产生的样本；
- `q_i(x)`：第 `i` 种已知 Trigger 产生的后门样本；
- `F_c,l(x)`、`F_b,l(x)`：干净模型、后门模型在第 `l` 层输出的特征；
- `m`：拟合 UAP 所诱导特征变化的模块；
- `n`：拟合 Trigger 所诱导特征变化的模块。

主比较为：

```text
干净模型 + 已知 UAP：
m(F_c,l(x)) ≈ F_c,l(p(x))

后门模型 + 已知 Trigger：
n(F_b,l(x)) ≈ F_b,l(q_i(x))
```

`m` 和 `n` 必须由已知输入映射 `p`、`q_i` 产生的成对特征监督得到，不能是任意搜索到的特征攻击方向。

## 3. 实验对象与配对要求

### 3.1 模型

- 干净模型与后门模型使用相同网络结构、数据处理和训练配置，模型参数可以不同；
- 优先使用同一随机种子配对训练的 clean/backdoor 模型；
- 正式实验至少使用 3 个分类器种子；
- 每个模型、checkpoint 和配置都记录唯一 ID 与文件哈希。

### 3.2 输入映射

- UAP `p` 应在干净模型上达到较高目标攻击成功率；
- Trigger `q_i` 应在其对应后门模型上达到较高目标攻击成功率；
- 同一次比较中，UAP 和 Trigger 应使用相同目标类别；
- 正式跨 Trigger 结论优先包含至少 3 种 Trigger；如果暂时只有 BadNets Patch，只能形成该类型内部的探索性结论。

### 3.3 四组实验单元

| 输入映射 | 干净模型 | 后门模型 | 作用 |
|---|---|---|---|
| UAP `p` | 主实验 | 交叉控制 | 观察 UAP 诱导的特征变化及模型差异 |
| Trigger `q_i` | 交叉控制 | 主实验 | 观察后门 Trigger 诱导的特征变化及模型差异 |

加入交叉控制是为了区分：观察到的复杂度差异究竟来自映射本身，还是仅仅来自 clean/backdoor 模型表征不同。

## 4. 实验一：观察浅层特征变化

### 4.1 实验目的

比较添加 UAP 和添加 Trigger 前后的浅层特征变化，回答：

- 哪些空间位置和通道发生了变化；
- 变化幅度有多大；
- 同一种映射在不同样本上的变化是否一致；
- 不同 Trigger 的变化在浅层是否比在像素空间更相似；
- Trigger 的变化是否仍能与 UAP 的变化区分。

### 4.2 变化量

```text
像素空间：
Δx_p   = p(x) - x
Δx_qi  = q_i(x) - x

浅层特征空间：
Δl_p   = F_c,l(p(x)) - F_c,l(x)
Δl_qi  = F_b,l(q_i(x)) - F_b,l(x)
```

所有变化都必须在同一个输入样本 `x` 上成对计算，不能拿不同样本直接相减。

### 4.3 浅层范围

以 CIFAR ResNet-18 为例，先扫描少量具有明确意义的切分点：

1. 像素输入，作为基准；
2. `stem` 输出；
3. `layer1.0` 输出；
4. `layer1` 输出；
5. `layer2` 输出；
6. `layer3` 输出，作为较深的负对照。

不需要枚举每个卷积层和激活层。使用验证集选择**最浅且满足条件的层**，随后冻结该层，再在测试集报告结果。

### 4.4 观测指标

- 相对 L1、L2 和均方变化：衡量变化大小；
- 空间热图和通道热图：观察变化位置；
- Top-k 能量占比、稀疏度和参与维度：观察变化是否集中；
- 同一映射跨样本的余弦相似度：观察方向是否稳定；
- 不同 Trigger 之间的余弦距离和线性 CKA：观察跨 Trigger 相似性；
- Trigger 间距离压缩比：

```text
R_l = 浅层特征空间中的 Trigger–Trigger 距离
      / 像素空间中的 Trigger–Trigger 距离
```

- Trigger 与 UAP 的留出集分类 AUROC：确认二者没有在浅层一起混为一类；
- 干净准确率、真实 Trigger ASR 和真实 UAP ASR：确认输入映射本身有效。

所有距离比较前，先对变化向量进行归一化，避免仅由数值尺度造成假相似。

### 4.5 实验一通过条件

实验一同时满足以下条件才算通过：

1. `R_l` 的 95% bootstrap 置信区间上界小于 1，说明不同 Trigger 的差异在浅层被压缩；
2. Trigger 与 UAP 的测试集 AUROC 不低于 0.80，说明二者仍可区分；
3. 结果在至少 3 个分类器种子上的方向一致；
4. 主实验中的真实 Trigger ASR 和真实 UAP ASR 均不低于 90%，且相差不超过 5 个百分点；
5. 不能只有接近分类头的深层通过，否则可能只是目标类别特征聚合，而不是可用的浅层结构。

如果 Trigger 和 UAP 在浅层一起变得不可区分，即使不同 Trigger 之间更相似，也不能认为实验一成功。

## 5. 实验二：拟合输入映射诱导的特征映射

### 5.1 实验目的

在实验一选出的浅层上，用相同的轻量模块族分别拟合 `m` 和 `n`，比较它们达到相同测试误差所需的最少 bit。

训练时：

- clean/backdoor 模型全部冻结；
- UAP 和 Trigger 全部冻结；
- 只拟合输入映射造成的特征变化；
- 优化目标只使用特征拟合误差；
- ASR 只用于检查拟合后的特征是否忠实复现原映射行为，不作为优化目标。

### 5.2 候选拟合模块

UAP 与 Trigger 必须使用完全相同的候选族：

| 等级 | 模块 |
|---|---|
| C0 | 逐通道仿射或常数通道偏移 |
| C1 | 可剪枝的固定加性残差 |
| C2 | rank 为 1、2、4、8 的 `1×1` 瓶颈残差模块 |
| C3 | rank 为 1、2、4、8 的单个 `3×3` 瓶颈残差块 |
| C4 | rank 为 1、2、4、8 的两个 `3×3` 瓶颈残差块 |

建议共同设置：

- 拟合随机种子：0、1、2；
- smoke/full 优化步数：250/2000；
- Adam，学习率 `1e-3`；
- 剪枝率：0、0.50、0.75、0.90、0.95；
- 量化：fp32、int8、int4。

具体参数可以在 smoke 后调整一次，但必须在正式实验前冻结，之后不能根据结果单独偏向 UAP 或 Trigger。

### 5.3 描述长度协议

建立固定协议 `MDL-FEATURE-v1`，实际 bit 至少包括：

- 层 ID、输入输出维度；
- 模块类型、rank、depth、kernel 等结构信息；
- 剪枝后非零位置或掩码；
- 量化方式、scale 等元数据；
- 实际保存的权重 bit；
- 解码候选所必需的其他信息。

模型、数据和双方共享的公共解码器不计入候选描述长度。搜索时间、GPU 时间、显存和查询次数单独报告，不计入 `L(m)` 或 `L(n)`。

### 5.4 复杂度定义

在误差阈值 `ε ∈ {0.10, 0.20, 0.30}` 下定义：

```text
Kε(p,l) = 满足测试集 NRMSE ≤ ε 且功能保真度通过的 m 的最少 bit
Kε(q,l) = 满足测试集 NRMSE ≤ ε 且功能保真度通过的 n 的最少 bit
```

功能保真度要求：

```text
|ASR_fit - ASR_source| ≤ 5 个百分点
```

其中 `ASR_fit` 是把拟合后的特征送入冻结模型后半段得到的 ASR，`ASR_source` 是真实 `p(x)` 或 `q_i(x)` 的 ASR。

### 5.5 实验二通过条件

主要判据使用 `ε=0.20`，并用 `ε=0.10`、`0.30` 做稳健性检查：

1. 3 个分类器种子都能得到有效的 `m` 和 `n`；不可达产生的 NaN 不能当作支持证据；
2. 每个种子至少 2 种 Trigger 满足：

```text
K0.20(p,l) / K0.20(q_i,l) ≥ 1.25
```

3. 在其他误差阈值下不出现稳定反转；
4. 主要复杂度差距大于同一映射跨 clean/backdoor 模型产生的表示偏移；
5. 结论来自留出测试集，并得到完整 bit–NRMSE 曲线支持，而不是只比较一个候选点。

## 6. 必要软件接口

| 接口 | 输入 | 输出与作用 |
|---|---|---|
| `KnownInputMapping.apply` | 输入 `x` | 返回固定的 `p(x)` 或 `q_i(x)` |
| `SplitClassifier.extract_features` | `x, layer` | 返回指定层特征 `z` |
| `SplitClassifier.forward_from_features` | `z, layer` | 从该层继续前向，用于验证拟合特征的 ASR |
| `FeatureMapping.forward` | 特征 `z` | 返回拟合后的特征 |
| `FeatureCodec.encode/decode/count_bits` | 候选模块 | 序列化、恢复并计算真实 bit |

第一条必须通过的单元测试是：

```text
forward_from_features(extract_features(x,l), l) ≈ forward(x)
```

此外还要测试：

- 模型、UAP 和 Trigger 在实验前后参数哈希不变；
- `x`、`p(x)`、`q_i(x)` 的样本 ID 严格对齐；
- 编解码往返后输出一致，`count_bits` 等于实际编码长度；
- 训练、验证、测试数据无泄漏；
- 候选结果原子写入，可断点续跑；
- smoke 结果明确标记为非正式结论。

## 7. 执行顺序

1. 检查模型、UAP、Trigger、目标标签、数据、环境、GPU 和文件哈希；
2. 运行最小 smoke：1 个种子、1 个 Trigger、1 个浅层、少量样本和极少优化步；
3. 运行实验一 pilot，检查热图、距离和统计量是否合理；
4. 运行实验一 formal，仅用验证集选择最浅合格层并冻结；
5. 在 seed 0 上运行实验二，检查 bit–NRMSE 曲线、功能保真度和交叉控制；
6. seed 0 通过后，在不同 GPU 上并行运行 seed 1、2；
7. 汇总 Markdown、JSON、图表和 Go/No-Go 结论。

服务器执行继续采用既有协作方式：Codex 在 Windows 本地仓库生成并提交代码，用户在服务器上执行 Bash 命令并回传结果。每次只推进当前一步。

长任务必须支持：

- `nohup` 后台运行；
- 独立日志、PID 和 run ID；
- 候选级原子写入；
- checkpoint 与断点续跑；
- 指定 `cuda:n` 或自动选择空闲 GPU。

## 8. 输出文件

实验一输出：

- 每层特征变化统计；
- 空间和通道热图；
- Trigger/Trigger、Trigger/UAP 相似度矩阵；
- 压缩比与 AUROC 随层变化曲线；
- 验证集选层记录。

实验二输出：

- 全部候选的配置、实际 bit、NRMSE 和 ASR；
- bit–NRMSE 曲线；
- 各误差阈值的最小 bit 表；
- 跨 Trigger、跨种子的排序一致性和反例；
- 主实验与交叉控制的复杂度差异。

共同输出：

- manifest、环境信息、Git commit、配置和协议版本；
- 日志、checkpoint 和逐步实验记录；
- 最终 Markdown 报告和 JSON 报告。

## 9. 最终判定

| 实验一 | 实验二 | 结论与下一步 |
|---|---|---|
| 通过 | 通过 | 浅层变化可比较，复杂度具有稳定判别力；进入反向特征搜索设计 |
| 通过 | 失败 | 浅层结构存在，但当前复杂度指标或候选模块缺乏判别力；先修改拟合族或编码假设 |
| 失败 | 不进行 | 浅层没有形成可用的共同结构，或 Trigger 与 UAP 一起塌缩；暂停特征空间路线 |
| 数据不足 | 数据不足 | 只报告探索性结果，补齐种子、Trigger 类型和正对照后再判断 |

本实验最多证明：在固定层、固定候选模块族、固定编码协议和固定数据分布下，已知后门 Trigger 所诱导的特征映射是否表现出更低的经验描述复杂度。它不能直接证明所有后门映射都比所有对抗映射简单，也不能从特征映射唯一恢复输入空间 Trigger。

## 10. 开始实现前需要确认的资产

- [ ] 至少一组配对的 clean/backdoor 模型；
- [ ] 后门模型对应的已知 Trigger 工件；
- [ ] 在 clean 模型上达到目标 ASR 的已知 UAP 工件；
- [ ] UAP 与 Trigger 使用相同目标类别；
- [ ] 训练、验证、测试样本 ID 清单；
- [ ] 模型与映射工件的文件哈希；
- [ ] 候选浅层、指标阈值、模块族和编码协议在正式实验前冻结。

首个里程碑是完成一组 BadNets Patch 与一组已知目标 UAP 的正向 smoke/pilot。只有源 ASR、模型切分、浅层统计和 bit 编码链路全部正确，才扩展到更多 Trigger 和正式实验。
